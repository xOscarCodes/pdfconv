"""pdfconv — PDF -> DOCX / Markdown converter.

A small package exposing a conversion engine (:mod:`pdfconv.engine`), a
customtkinter GUI (:mod:`pdfconv.gui`) and a headless CLI (:mod:`pdfconv.cli`),
all sharing the same engine. The entry point is ``pdf2docx_app.py`` at the
repository root.
"""

__version__ = "1.0.0"
__all__ = ["__version__"]
